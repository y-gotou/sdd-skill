# タスク管理設定(プロジェクト固有)

## 恒久仕様書
<!-- 例: docs/01_要件・仕様.md。無い場合は「なし(スキップ)」または「なし(初回タスクで作成)」 -->
- パス:
- 形式: <!-- 章立てあり(delta 反映可) / 不定形(自由記述で反映) -->

## 検証系統
<!-- このプロジェクトで利用できる検証手段の一覧。plan.md の検証方法はここから選ぶ。
     例: 静的確認(差分レビュー) / 単体テスト(dotnet test) / 実機ワークフロー確認(実環境・ユーザー実施) -->
-

## リリース運用
<!-- ブランチ・マージ・デプロイとタスク管理の関係。各項目とも該当を選んで記載 -->
- タスク文書の載せ方: <!-- 実装ブランチに同梱(実装 PR でマージ) / main へ直接コミット -->
- ブランチ運用: <!-- 作業ブランチの作成タイミング(起票時 / 着手時)、命名規則(例: feat/T-NNN-件名)、票の起票先(main で起票してから作業ブランチを作成 / 作業ブランチ上で起票) -->
- main への同期(同梱の場合): <!-- 全状態変化で即時(既定・references/03-work.md §5) / 粒度を変える場合はここに記載(例: 各ゲート承認時のみ / セッション終了時 / blocked 時のみ)。保護ブランチでは docs 用 PR(まとめ可)。G3 後の完了記録は「完了記録の載せ方」による -->
- 仕様書反映のタイミング: <!-- 実装 PR に同梱し G3 で追認 / G3 承認後に反映 -->
- G3 の時期: <!-- マージ・本番反映の確認後 / エージェント検証消化後(マージ・反映を待たない) -->
- 完了記録の載せ方(保護ブランチの場合): <!-- タスクごとに即時 PR / 複数タスク分をまとめて定期的に PR(まとめる上限を併記。例: 週次 / 3タスクごと)。コミットごとに CI・デプロイが走る環境では後者が適する -->

## 固有規約
<!-- タスク管理に関するプロジェクト固有の追加規則があれば記載 -->
-
